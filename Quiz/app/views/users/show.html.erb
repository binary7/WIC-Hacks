<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Score:</strong>
  <%= @user.userScore(@user.score.total) %>
  puts @user.score.total
</p>

<p>
  <strong>Rec:</strong>
  <%= @user.userType(@user.userScore(@user.score.total)) %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
